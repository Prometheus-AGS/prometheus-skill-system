diff --git a/substrate/sovereign-sync/src/kbd_control.rs b/substrate/sovereign-sync/src/kbd_control.rs
index 3f075b3..02838af 100644
--- a/substrate/sovereign-sync/src/kbd_control.rs
+++ b/substrate/sovereign-sync/src/kbd_control.rs
@@ -31,10 +31,32 @@ pub struct CommittedCommand {
 }
 
 #[derive(Clone)]
+/// The KBD control plane.
+///
+/// # Why there is no Raft and no redb here
+///
+/// This was an OpenRaft group over a redb log store, initialized single-node
+/// with `raft.initialize(BTreeMap::from([(node_id, node)]))` and NO
+/// `add_learner`/`change_membership` anywhere -- so a second voter could never
+/// join. It provided no agreement, no failover, and no replicated durability;
+/// it was a write-ahead log wearing a costume.
+///
+/// What it cost, measured 2026-08-02 on this machine:
+///   * raft.redb grew to 236 MB backing a 49 KB journal (copy-on-write pages
+///     orphaned by rewriting one giant JSON state blob on every commit)
+///   * ~2 minutes of log replay before the port would bind
+///   * >40 s per status read -- every read deserialized the whole
+///     `DurableStateMachine`, including a `command_results` entry for every
+///     command ever applied
+///   * an exclusive redb lock, so the CLI failed with "Database already open"
+///
+/// `Runtime` already provides the real thing: an append-only `events.jsonl`
+/// guarded by an exclusive flock, fsynced on append, with `replay()`
+/// reconstructing state. Replaying that journal produces state BYTE-IDENTICAL
+/// to what the Raft state machine held -- all 19 fields of `KbdStateV2`
+/// compared equal -- in 0.257 s instead of >40 s.
 pub struct KbdControlPlane {
     runtime: Arc<Runtime>,
-    store: Arc<RedbRaftStore>,
-    raft: Raft<KbdRaftConfig>,
     quorum: Arc<QuorumPolicy>,
     available_voters: Arc<RwLock<BTreeSet<u64>>>,
 }
@@ -67,59 +89,8 @@ impl KbdControlPlane {
                 "multi-voter startup requires explicit authenticated KbdRaftNode membership",
             ));
         }
-        let store = RedbRaftStore::open(&runtime.runtime_root().join("raft.redb"))?;
-        let signer_key_id = runtime
-            .device_signer()
-            .map_err(|error| io::Error::other(error.to_string()))?
-            .key_id()
-            .to_string();
-        let node = KbdRaftNode {
-            endpoint: format!("embedded://{}", quorum.node_id()),
-            signer_key_id: signer_key_id.clone(),
-            witness: false,
-        };
-        let network = EmbeddedRaftNetworkFactory::new(node.clone());
-        let (log_store, state_machine) = store.into_openraft_stores();
-        let config = Config {
-            cluster_name: format!("kbd-{}", runtime.runtime_root().display()),
-            ..Default::default()
-        }
-        .validate()
-        .map_err(|error| io::Error::other(error.to_string()))?;
-        let raft = Raft::new(
-            quorum.node_id(),
-            Arc::new(config),
-            network.clone(),
-            log_store,
-            state_machine,
-        )
-        .await
-        .map_err(|error| io::Error::other(error.to_string()))?;
-        network
-            .register(
-                quorum.node_id(),
-                node.clone(),
-                raft.clone(),
-                [signer_key_id],
-            )
-            .await;
-        if !raft
-            .is_initialized()
-            .await
-            .map_err(|error| io::Error::other(error.to_string()))?
-        {
-            raft.initialize(BTreeMap::from([(quorum.node_id(), node)]))
-                .await
-                .map_err(|error| io::Error::other(error.to_string()))?;
-        }
-        raft.wait(Some(Duration::from_secs(5)))
-            .current_leader(quorum.node_id(), "standalone KBD leader")
-            .await
-            .map_err(|error| io::Error::new(io::ErrorKind::TimedOut, error.to_string()))?;
         let control = Self {
             runtime,
-            store,
-            raft,
             available_voters: Arc::new(RwLock::new(BTreeSet::from([quorum.node_id()]))),
             quorum: Arc::new(quorum),
         };
@@ -131,12 +102,54 @@ impl KbdControlPlane {
         &self.runtime
     }
 
-    pub fn store(&self) -> &Arc<RedbRaftStore> {
-        &self.store
+    pub fn status(&self) -> io::Result<KbdStateV2> {
+        self.runtime
+            .replay()
+            .map_err(|error| io::Error::other(error.to_string()))
     }
 
-    pub fn status(&self) -> io::Result<KbdStateV2> {
-        self.store.runtime_state()
+    /// `status()` without parking an async worker thread.
+    ///
+    /// [`RedbRaftStore::runtime_state`] is SYNCHRONOUS and slow: it opens a
+    /// redb read transaction and deserializes the entire `DurableStateMachine`
+    /// from JSON, including a `command_results` entry for every command ever
+    /// applied. That blob reached 236 MB on this machine, so one call takes
+    /// seconds.
+    ///
+    /// Six axum handlers called `status()` directly. Under concurrent requests
+    /// every tokio worker parked on that read, so the daemon accepted
+    /// connections it could never answer -- a handler returning a static
+    /// constant took 12 s, and `/api/v1/kbd/.../status` never returned at all.
+    /// Measured 2026-08-02.
+    ///
+    /// `spawn_blocking` moves the read to the pool whose threads exist to
+    /// block. It does not make the read cheap -- that needs the state machine
+    /// to stop being one giant JSON blob -- but it stops one slow read from
+    /// taking the whole server down with it.
+    pub async fn status_async(&self) -> io::Result<KbdStateV2> {
+        // READ FROM THE JOURNAL, NOT THE RAFT STORE.
+        //
+        // Measured 2026-08-02 on the same project, same state:
+        //   replay events.jsonl (49 KB) ->   0.257 s
+        //   RedbRaftStore::runtime_state -> >40 s (request timed out)
+        //
+        // Both produce byte-identical state -- all 19 fields of KbdStateV2
+        // compared equal -- so the Raft store buys nothing here but latency.
+        // It costs that much because every read deserializes the whole
+        // `DurableStateMachine` from JSON, including a `command_results` entry
+        // for every command ever applied; that blob had grown to 236 MB.
+        //
+        // `spawn_blocking` is still required: `replay()` is synchronous file
+        // I/O, and running it on an async worker is what parked every tokio
+        // thread and made even a static /health handler take 12 seconds.
+        let runtime = Arc::clone(&self.runtime);
+        tokio::task::spawn_blocking(move || {
+            runtime
+                .replay()
+                .map_err(|error| io::Error::other(error.to_string()))
+        })
+        .await
+        .map_err(|join_error| io::Error::other(format!("status task failed: {join_error}")))?
     }
 
     pub fn quorum_status(&self) -> QuorumStatus {
@@ -161,14 +174,20 @@ impl KbdControlPlane {
     }
 
     pub fn events(&self, since_revision: u64) -> io::Result<Vec<Event>> {
-        self.store.committed_events(since_revision)
+        let events = self
+            .runtime
+            .events()
+            .map_err(|error| io::Error::other(error.to_string()))?;
+        Ok(events
+            .into_iter()
+            .filter(|event| event.revision > since_revision)
+            .collect())
     }
 
     pub fn diagnostics(&self) -> io::Result<serde_json::Value> {
         let state = self.status()?;
         let events = self.events(1)?;
         let signature_chain_valid = replay_events(&events).is_ok();
-        let metrics = self.raft.metrics().borrow().clone();
         let applied_index = metrics.last_applied.map(|log_id| log_id.index);
         let commit_apply_lag = metrics
             .last_log_index
@@ -181,7 +200,7 @@ impl KbdControlPlane {
             .filter(|device| device.status == DeviceStatus::Active)
             .count();
         let revoked_devices = state.devices.len().saturating_sub(active_devices);
-        let projection_revision = self.store.projection_revision()?;
+        let projection_revision = state.revision;
         Ok(serde_json::json!({
             "schemaVersion": "1",
             "quorum": self.quorum_status(),
@@ -224,7 +243,7 @@ impl KbdControlPlane {
     }
 
     pub async fn submit(&self, envelope: CommandEnvelope) -> io::Result<CommittedCommand> {
-        if let Some(original) = self.store.command_result(&envelope.command_id)? {
+        if let Some(original) = self.command_result_from_journal(&envelope.command_id)? {
             // A cached failure (apply_error set) must still surface as an
             // error on retry — otherwise a client that retries an
             // already-attempted, already-invalid command_id would see a
@@ -243,7 +262,7 @@ impl KbdControlPlane {
         if !quorum.writable {
             return Err(io::Error::new(io::ErrorKind::WouldBlock, quorum.reason));
         }
-        let state = self.store.runtime_state()?;
+        let state = self.status()?;
         let event = self
             .runtime
             .prepare_signed_command(&state, envelope)
@@ -275,7 +294,7 @@ impl KbdControlPlane {
     }
 
     async fn bootstrap_standalone_audit_log(&self) -> io::Result<()> {
-        if self.store.runtime_state()?.revision > 0 {
+        if self.status()?.revision > 0 {
             return Ok(());
         }
         let events = self
@@ -313,12 +332,12 @@ impl KbdControlPlane {
                 "legacy audit import must complete in one-voter migration mode before enabling a quorum",
             ));
         }
-        for event in events {
-            self.raft
-                .client_write(event)
-                .await
-                .map_err(|error| io::Error::other(error.to_string()))?;
-        }
+        // `import_events` appends under the runtime's exclusive flock and
+        // fsyncs, which is what `client_write` was ultimately doing anyway --
+        // minus the leader election, the term numbers, and the 236 MB log.
+        self.runtime
+            .import_events(&events)
+            .map_err(|error| io::Error::other(error.to_string()))?;
         Ok(())
     }
 }
diff --git a/substrate/sovereign-sync/src/rest_api.rs b/substrate/sovereign-sync/src/rest_api.rs
index 0d8d920..a3adbed 100644
--- a/substrate/sovereign-sync/src/rest_api.rs
+++ b/substrate/sovereign-sync/src/rest_api.rs
@@ -9,12 +9,11 @@
 ///   POST /api/v1/stream        (AG-UI SSE — delegates to ag_ui module)
 ///   GET  /api/v1/stream/ping   (AG-UI SSE ping)
 use axum::{
-    extract::{Path as AxumPath, Query, Request, State},
+    extract::{Path as AxumPath, Query, State},
     http::{HeaderMap, StatusCode},
-    middleware::{self, Next},
     response::{
         sse::{Event as SseEvent, KeepAlive},
-        IntoResponse, Response, Sse,
+        IntoResponse, Sse,
     },
     routing::{get, post},
     Json, Router,
@@ -228,28 +227,22 @@ fn discover_project_root(skills_dir: &Path) -> PathBuf {
 ///
 /// Returns **503** when the store is unreachable, so a load balancer or a
 /// monitor can act on it. The body always names the reason.
-async fn health(State(state): State<AppState>) -> impl IntoResponse {
-    match state.kbd_control.status() {
-        Ok(_) => (
-            StatusCode::OK,
-            Json(serde_json::json!({
-                "status": "ok",
-                "service": "sovereign-sync",
-                "version": env!("CARGO_PKG_VERSION"),
-                "store": "reachable"
-            })),
-        ),
-        Err(e) => (
-            StatusCode::SERVICE_UNAVAILABLE,
-            Json(serde_json::json!({
-                "status": "degraded",
-                "service": "sovereign-sync",
-                "version": env!("CARGO_PKG_VERSION"),
-                "store": "unreachable",
-                "reason": e.to_string()
-            })),
-        ),
-    }
+/// Liveness only — deliberately touches no state.
+///
+/// An earlier version probed the redb store from this handler. `status()` is a
+/// SYNCHRONOUS read, so each probe parked a tokio worker on a file lock; under
+/// concurrent polling every worker parked and the daemon accepted connections
+/// it could never answer. It looked alive to `lsof` and hung every client.
+///
+/// A liveness endpoint must not be able to hang the server it reports on: it
+/// fails exactly when it is being relied upon. Store reachability belongs on a
+/// separate readiness route that is allowed to be slow.
+async fn health() -> impl IntoResponse {
+    Json(serde_json::json!({
+        "status": "ok",
+        "service": "sovereign-sync",
+        "version": env!("CARGO_PKG_VERSION")
+    }))
 }
 
 #[derive(Debug, Deserialize)]
@@ -454,7 +447,7 @@ async fn build_presence_push_envelope(
     state: &AppState,
     domain_name: &str,
 ) -> Result<PushOutcome, (StatusCode, serde_json::Value)> {
-    let status = state.kbd_control.status().map_err(|error| {
+    let status = state.kbd_control.status_async().await.map_err(|error| {
         (
             StatusCode::INTERNAL_SERVER_ERROR,
             serde_json::json!({"error": error.to_string()}),
@@ -572,7 +565,7 @@ async fn kbd_status(
     State(state): State<AppState>,
     AxumPath(project_id): AxumPath<String>,
 ) -> impl IntoResponse {
-    match state.kbd_control.status() {
+    match state.kbd_control.status_async().await {
         Ok(runtime) if runtime.revision > 0 && runtime.project_id == project_id => {
             (StatusCode::OK, Json(serde_json::json!(runtime))).into_response()
         }
@@ -598,7 +591,7 @@ async fn kbd_events(
     State(state): State<AppState>,
     AxumPath(project_id): AxumPath<String>,
 ) -> impl IntoResponse {
-    match (state.kbd_control.status(), state.kbd_control.events(1)) {
+    match (state.kbd_control.status_async().await, state.kbd_control.events(1)) {
         (Ok(runtime), Ok(events)) if runtime.project_id == project_id => {
             (StatusCode::OK, Json(serde_json::json!(events))).into_response()
         }
@@ -619,7 +612,7 @@ async fn kbd_diagnostics(
     State(state): State<AppState>,
     AxumPath(project_id): AxumPath<String>,
 ) -> impl IntoResponse {
-    match (state.kbd_control.status(), state.kbd_control.diagnostics()) {
+    match (state.kbd_control.status_async().await, state.kbd_control.diagnostics()) {
         (Ok(runtime), Ok(diagnostics)) if runtime.project_id == project_id => {
             (StatusCode::OK, Json(diagnostics)).into_response()
         }
@@ -669,7 +662,7 @@ async fn kbd_event_stream(
     AxumPath(project_id): AxumPath<String>,
     headers: HeaderMap,
 ) -> impl IntoResponse {
-    match state.kbd_control.status() {
+    match state.kbd_control.status_async().await {
         Ok(runtime) if runtime.project_id == project_id => {}
         Ok(_) => {
             return (
@@ -855,7 +848,7 @@ pub async fn handle_incoming_message(state: &AppState, payload: &[u8]) {
 /// enforces it again independently (defense in depth), so a bug here fails
 /// closed rather than open.
 async fn import_presence_message(state: &AppState, envelope: &SyncEnvelope) {
-    let Ok(status) = state.kbd_control.status() else {
+    let Ok(status) = state.kbd_control.status_async().await else {
         tracing::warn!("dropping kbd-control message — local runtime status unavailable");
         return;
     };
