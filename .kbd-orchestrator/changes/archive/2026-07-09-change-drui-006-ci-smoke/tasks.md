<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-drui-006-ci-smoke

- [x] 1 Locally verify prometheus-research --mode server boots cleanly and /health, /, /static/htmx.min.js, /brand/primary-light.svg all return 200 with non-empty bodies
- [x] 2 Add a ci-smoke job to .github/workflows/prometheus-research.yml: build release binary, start --mode server in background, poll /health with a timeout
- [x] 3 In the ci-smoke job, curl /, /static/htmx.min.js, /brand/primary-light.svg asserting HTTP 200 + non-empty body for each, then tear down the server process
- [x] 4 Validate the updated workflow YAML (yamllint or python yaml.safe_load) and confirm the new job doesn't disturb the existing fmt/clippy/test matrix
