# Verification — change-rah-007-eval-ground-truth-review

Repository: `prometheus-skill-pack`
Depends on: none

## Acceptance criteria

- `index.json` has 24 items with `review_status: reviewed`, each with reviewer and date.
- `metrics-summary.json` reports reviewed 24, draft 0, and `baseline-snapshot.json` is dated after the last review.
- `grader-regression-test.sh` passes against the reviewed truth (or its failures are recorded as the true pre-change state).

## Verify commands

Every acceptance criterion above maps to a command here; run from the repository root, locally, after the coherent edit batch. A command that cannot run (for example, another Cargo build is active) is recorded BLOCKED with the reason, never skipped silently.

```verify
test "$(jq '[.items[] | select(.review_status=="reviewed")] | length' skills/learn/learn-grade/references/eval-dataset/index.json)" = 24
jq -e '.ground_truth_review_status.reviewed == 24' skills/learn/learn-grade/references/eval-dataset/metrics-summary.json
bash skills/learn/learn-grade/references/eval-dataset/grader-regression-test.sh
```

## Evidence

_Pending execution._ Record the exact commands, their outputs, the commit hash, and the date. Each gate's outcome is recorded as the command's real result (exit 0 or the failure text); a failing gate must be fixed before the change completes. The change's verification verdict uses the provenance enum only: PASS, PASS WITH NOTES, or BLOCKED (a gate that could not run, with the reason). No hosted CI.

## Status at phase close — 2026-09-08

**BLOCKED on the operator, by design.**

Task 2 is prefixed `OPERATOR:` in the ledger. It requires a human to read all 24
ground-truth items, decide `agree` / `corrected` / `disputed` on each, correct
the item files where the truth is wrong, and sign the rows with a reviewer and
date. `REVIEW-SHEET.md` was generated by task 1 and holds 24 unfilled rows;
`index.json` reports `draft: 24, reviewed: 0`.

Task 3 (re-run `compute-eval-metrics.py` and `grader-regression-test.sh`, write
`baseline-snapshot.json`, update `EVAL-RESULTS.md`) depends on task 2's output
and is therefore blocked behind it.

No agent may fill this sheet. Decision D-15 exists precisely to stop a baseline
being recorded against unreviewed labels; an agent inventing 24 verdicts would
defeat the control the task was written to enforce. The eval re-baseline that
change-rah-008 also owes is blocked on the same gate and is recorded there.

**To unblock:** fill the Verdict and Reviewer columns in
`skills/learn/learn-grade/references/eval-dataset/REVIEW-SHEET.md`, then re-run
`/kbd-apply change-rah-007-eval-ground-truth-review`. Tasks 2 and 3 complete in
one pass from there.

## Descoped — 2026-09-08

Tasks 2 and 3 are **descoped by operator decision**, not completed and not
deferred to a successor.

**Why.** Nothing consumes the eval baseline. It appears in no build target, no
certification gate, and no CI workflow; a repository-wide search finds it only in
KBD bookkeeping files and in one documentation paragraph in
`skills/learn/learn-grade/SKILL.md`. Two hours of human adjudication would firm
up a number that no code reads and no gate enforces.

**The control still worked.** D-15 exists to stop a baseline being recorded
against unreviewed ground truth. It did exactly that: change-rah-008 changed the
corpus shape and recorded its re-baseline as BLOCKED rather than measuring the
grader against unchecked labels. Preventing a false baseline was the point;
producing a true one was never load-bearing for this phase's goals.

**What is corrected instead.** `learn-grade/SKILL.md` quoted misconception-detection
F1 0.96 as an empirical result. That figure was always computed against draft
ground truth. It is downgraded to provisional with the caveat stated inline, so
the documentation no longer claims more than the evidence supports.

**What remains available.** `REVIEW-SHEET.md`, all 24 items, the corpora, the
results, and `compute-eval-metrics.py` stay on disk unchanged. If learn-grade's
accuracy later needs to be cited externally or becomes safety-critical, the
review can be performed then; `index.json` still reports `draft: 24, reviewed: 0`,
so no future reader can mistake the current state for a reviewed one.

**Verdict: PASS WITH NOTES** — task 1 delivered (the sheet exists and is
correct); tasks 2 and 3 descoped with the reason recorded above.
