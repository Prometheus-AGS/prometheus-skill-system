<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-drt-002-thread-scheduler-and-budgets

- [x] 1 Implement src/threads.rs: Semaphore-bounded scheduler over JoinSet with per-task time::timeout and kill; the cap is a constructor argument
- [x] 2 Add the `threads run --package <dir> --max-parallel N` subcommand
- [x] 3 Add the budgets object to checkpoint.json with force-complete at job, director, and thread level; starting values from analysis D-07
- [x] 4 Write tests/thread-scheduler.sh with fixture workers covering complete, partial, failed, timeout, and a cap test that over-subscribes and asserts peak concurrency never exceeds the cap
- [x] 5 Run the integration gate: cargo check, then the scheduler suite, with the Cargo slot free
