<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-drt-004-deterministic-merge

- [x] 1 Implement scripts/merge-threads.sh: union sources by canonical URL into the existing stage 02/04 shapes, copy chunks into the stage 03 shape, union claims by content-addressed id keeping every provenance tuple, rewrite markers and emit citation-map.json
- [x] 2 Make the merge refuse, as CRITICAL, any dossier source absent from that thread's sources.json
- [x] 3 Extract canonical URL normalisation into the shared lib so stage 02 and the merge cannot drift
- [x] 4 Write tests/merge-threads.sh with overlapping sources and duplicate claims; assert determinism by hash across two runs, assert the unmodified driver validators pass on merged output, assert one citation number per document, assert the no-search violation fails
- [x] 5 Run the integration gate: merge suite plus the unmodified driver contract suite
- [x] 6 Regenerate the distribution after editing run-research.sh and pass check:distribution and validate:codex in this change (C-01; a desynced driver is defect D-B)
