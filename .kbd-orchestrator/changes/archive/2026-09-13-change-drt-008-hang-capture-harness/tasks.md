<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-drt-008-hang-capture-harness

- [x] 1 Implement tests/hang/run-hang-capture.sh: loop driver-contract.sh --scenario full-run for all --runs K (default 12; capture-and-continue on hang, --stop-on-hang opt-in); per-run scoped pre-run cleanup (kill prior PID trees, reset scratch), timeout (default 90s), timestamped logs, run record (duration, exit, assertions, load average); on timeout capture ps tree + macOS sample stacks + output-volume observation BEFORE scoped teardown
- [x] 2 Execute the capture campaign (all K runs executed; a hang is captured and the campaign continues, post-capture runs marked perturbed) and write HANG-CAPTURE.md: per-run table (duration, exit, assertions, load), measured failure rate hangs/K, and on the first hang the exact blocked command with its captured stack and the output-volume observation against the ~64 KB pipe-buffer hypothesis
- [x] 3 Goal-2 dating: establish pre-existing vs introduced and first appearance for the isolated hang site, naming the method on a method: line (commit-pass | merge-base | uncommitted-only) with its evidence and limitations; state explicitly that committed history cannot date code that exists only in this phase's uncommitted tree
