<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-goal-014

- [x] 1 Add optional `phases[]` to `loop-definition.schema.json`: array of `{name, type, stopping_condition, human_gate}`
- [x] 2 Add optional `goal_slug` field: link to `.kbd-orchestrator/goals/<slug>/goal.json`
- [x] 3 Verify backward compatibility: existing `loop.json` files without new fields still validate
- [x] 4 Update `loop-schema.md`: document new fields, add examples, cross-reference `goal.schema.json`
- [x] 5 Update `kbd-goal-start.sh` (goal-002): write `loop.json` to `.kbd-orchestrator/loops/<slug>/` alongside `goal.json`, so `/loop-tick` can drive goal advancement
- [x] 6 Test: run `jq -e` schema validation on existing loop.json files to confirm no regressions
