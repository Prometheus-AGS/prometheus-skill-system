<!-- GENERATED by kbd-apply from tasks.json — do not edit; edits are overwritten. -->

# Tasks — change-learn-004b

- [x] 1 Define `StorageProvider` trait with `read`, `write`, `merge`, `list`, and `watch` methods
- [x] 2 Define `CrdtEngine` trait with `apply`, `merge_docs`, and `export_state` methods
- [x] 3 Implement `LocalDirAdapter` (read/write JSON files from a configurable base path)
- [x] 4 Implement `automerge-rs` `CrdtEngine` backed by the schema from change-learn-001
- [x] 5 Add `IrohDocsAdapter` stub with `unimplemented!()` bodies and a TODO comment linking the Iroh Docs RFC
