<!-- generated by kbd-runtime; source revision 1186 -->
# deep-research-onyx-parity tasks

## change-drt-001-dispatch-smoke-and-thread-contracts — Prove concurrent subagent dispatch, then freeze the thread artifact contracts

- [x] 1 — Write tests/dispatch-smoke.sh and its fixture: dispatch two workers, assert both complete, assert wall-clock overlap, assert neither sees the other's scratch marker (COMPLETE)
- [x] 2 — RUN the smoke test and record the verdict. If dispatch is serial or isolation fails, STOP and record that D-03a's fallback applies (strategy B becomes primary, drt-007 becomes blocking) (COMPLETE)
- [x] 3 — Author references/thread-contracts.md with the normative shapes for brief, dossier, reflections, sources, claims (with the verbatim quote span) and threads/index.json (COMPLETE)
- [x] 4 — Write the two JSON schemas and the two templates; validate the fixture artifacts against them (COMPLETE)
- [x] 5 — Write tests/thread-contracts.sh: validate the fixture thread artifacts against their schemas and assert every claim's verbatim quote is non-empty and traceable to a source in the same thread's sources.json (COMPLETE)

## change-drt-007-install-surface-repair — Repair the two install-surface defects the predecessor's evidence run found

- [x] 1 — Add EnvironmentVariables with a __PROMETHEUS_PATH__ placeholder to the research plist template, matching the shared/launchagents convention, and extend the sed in install-binaries.sh to substitute it (COMPLETE)
- [x] 2 — Confirm by command that the research plist is NOT a services-manifest input (generate-service-manifest.mjs reads only shared/launchagents and shared/systemd), record the correction to analysis D-11b in the spec, and raise the migration question rather than silently moving the file (COMPLETE)
- [x] 3 — Add a daemon self-check reporting the resolved harness path and driver path with sizes, so a stale install is visible before a job is started (COMPLETE)
- [x] 4 — Republish the plugin generation so the installed driver matches the repo; prove the generator idempotent and pass the drift validators (C-01) (COMPLETE)
- [x] 5 — Write tests/installed-service-smoke.sh: render and install the plist, bootstrap under launchd, start a fixture job, assert a non-null harness_pid; AND install a marker-less stub driver fixture and assert the daemon health output reports it stale with both paths and sizes; then record the repair against D-A and D-B (COMPLETE)

## change-drt-002-thread-scheduler-and-budgets — Bounded thread scheduler with a code-enforced concurrency cap and budgets at every level

- [x] 1 — Implement src/threads.rs: Semaphore-bounded scheduler over JoinSet with per-task time::timeout and kill; the cap is a constructor argument (COMPLETE)
- [x] 2 — Add the `threads run --package <dir> --max-parallel N` subcommand (COMPLETE)
- [x] 3 — Add the budgets object to checkpoint.json with force-complete at job, director, and thread level; starting values from analysis D-07 (COMPLETE)
- [x] 4 — Write tests/thread-scheduler.sh with fixture workers covering complete, partial, failed, timeout, and a cap test that over-subscribes and asserts peak concurrency never exceeds the cap (COMPLETE)
- [x] 5 — Run the integration gate: cargo check, then the scheduler suite, with the Cargo slot free (COMPLETE)

## change-drt-004-deterministic-merge — Fold threads back into the existing stage 02/03/04 artifacts, deterministically

- [ ] 1 — Implement scripts/merge-threads.sh: union sources by canonical URL into the existing stage 02/04 shapes, chunks into stage 03, claims by content-addressed id, and write citation-map.json (IN_PROGRESS)
- [x] 2 — Make the merge refuse, as CRITICAL, any dossier source absent from that thread's sources.json (COMPLETE)
- [x] 3 — Extract canonical URL normalisation into the shared lib so stage 02 and the merge cannot drift (COMPLETE)
- [x] 4 — Write tests/merge-threads.sh with overlapping sources and duplicate claims; assert determinism by hash across two runs, assert the unmodified driver validators pass on merged output, assert one citation number per document, assert the no-search violation fails (COMPLETE)
- [x] 5 — Run the integration gate: merge suite plus the unmodified driver contract suite (COMPLETE)
- [x] 6 — Regenerate the distribution after editing run-research.sh and pass check:distribution and validate:codex in this change (C-01; a desynced driver is defect D-B) (COMPLETE)

## change-drt-003-director-worker-agents — A director that cannot search and workers that cannot see each other

- [x] 1 — Author agents/research-director.md: tools Read/Grep/Glob/Write only, cycle loop, brief dispatch, dossier reading, coverage+gap ledger append, never searches (COMPLETE)
- [x] 2 — Author agents/research-worker.md: tools WebSearch/WebFetch/Read/Write, writes only under threads/<tid>/, reflection before every search after the first, stop conditions, dossier rule written from the mechanism not from Onyx prose (COMPLETE)
- [x] 3 — Wire stage 02 in run-research.sh to invoke BOTH the scheduler and the merge (never one without the other); document the two-level rule and why a third level is refused in SKILL.md (COMPLETE)
- [x] 4 — Regenerate the skills index for the SKILL.md edit and run the driver contract suite unchanged (C-01) (COMPLETE)
- [x] 5 — Run the integration gate: driver contract suite plus strict validation of the edited skill (COMPLETE)
- [ ] 6 — Regenerate the distribution after editing run-research.sh and pass check:distribution and validate:codex in this change (C-01; a desynced driver is defect D-B) (PENDING)

## change-drt-005-multipass-report — Split stage 09 into outline, parallel sections, assembly, and edit, bound by a claim-set invariant

- [x] 1 — Author agents/outline-architect.md and the report-outline schema and template: sections to sub-questions to claim ids, word budget per section, CLAIM IDS ONLY — citation numbers are assigned solely by drt-004's merge and resolved at assembly time (COMPLETE)
- [x] 2 — Author agents/section-writer.md (writes only its own section file, receives only its assigned claims and excerpts) and agents/coherence-editor.md (Read+Edit, may not introduce claims) (COMPLETE)
- [x] 3 — Implement scripts/assemble-report.sh: concatenate sections, fail on orphan markers, missing references, claim-set drift, or a label described more strongly than it is (COMPLETE)
- [x] 4 — Wire stage 09 to the four passes with the post-edit re-assembly check; keep report-synthesizer.md as the direct-scale fallback; log any removed claim ids to the plan decision log (COMPLETE)
- [x] 5 — Write tests/report-assembly.sh covering orphan marker, claim-set drift, label misuse, and the editor-may-only-remove rule (COMPLETE)
- [x] 6 — Regenerate the skills index for the SKILL.md edit and run the integration gate (COMPLETE)

## change-drt-006-bench-and-metrics — Make "on par with Onyx" falsifiable: adopt the RACE criteria, build FACT and verified-claim ratio

- [ ] 1 — Vendor the adopted criteria, English criteria prompt, and a 10-task query subset pinned at upstream commit 469cce54; write ATTRIBUTION.md per Apache-2.0 (IN_PROGRESS)
