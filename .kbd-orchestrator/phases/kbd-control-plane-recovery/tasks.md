<!-- generated by kbd-runtime; source revision 381 -->
# kbd-control-plane-recovery tasks

## repair-kbd-memory-rest-contract — Make KBD memory logging and recall use the installed surreal-memory service contract

- [x] 1 — 1.1 Confirm the installed memory service origin and canonical routes with local health/search probes, and record the results in the change execution evidence. (COMPLETE)
- [x] 2 — 1.2 Add explicit-override-first local endpoint discovery plus MCP-to-REST base normalization, and verify focused detection tests cover configured, canonical, cached, and unreachable cases. (COMPLETE)
- [x] 3 — 1.3 Migrate lifecycle writes to the current entity payload/route with bounded diagnostics, and verify a fake server receives a valid string-observation entity without blocking hook execution. (COMPLETE)
- [x] 4 — 1.4 Migrate recall to supported entity search with deterministic same-project/token/recency ranking, and verify reachable-match, reachable-empty, and unreachable digests. (COMPLETE)
- [x] 5 — 2.1 Update the recall skill and all owning memory integration references to the canonical health, entity, and search contracts, and verify no obsolete route remains in that surface. (COMPLETE)
- [x] 6 — 2.2 Run the memory smoke suite, `/bin/bash` 3.2 syntax/execution checks for touched scripts, strict skill validation, and `git diff --check`, recording exact local results. (COMPLETE)

## harden-uiux-routing-discovery — Make UI/UX routing existing-target-first and capability-aware

- [x] 1 — 1.1 Update the managed UI/UX routing template to require an existing incumbent target before Impeccable context loading, and verify future-path versus existing-target behavior is unambiguous in the rendered block. (COMPLETE)
- [x] 2 — 1.2 Replace unconditional `ux-designer` attribution with an installed-capability check and named fallback, and verify the roster no longer claims unverifiable Anthropic provenance. (COMPLETE)
- [x] 3 — 1.3 Update the injector skill documentation for the `uiux-routing` pack and verify its documented flags match the script. (COMPLETE)
- [x] 4 — 2.1 Add or extend managed-fence tests to prove surrounding instructions are byte-preserved and a second injection is idempotent. (COMPLETE)
- [x] 5 — 2.2 Dry-run the refreshed pack against a temporary target and resolve the UAR incumbent A2UI page through Impeccable read-only, verifying the UAR worktree status is unchanged. (COMPLETE)
- [x] 6 — 2.3 Run strict skill validation and `git diff --check`, recording exact local results. (COMPLETE)

## add-kbd-registry-prune — Add explicit recoverable cleanup for missing KBD replica registrations

- [x] 1 — 1.1 Add structured missing-registration and prune-report types plus dry-run inventory to `ProjectRegistry`, and verify existing and missing paths are classified correctly in focused tests. (COMPLETE)
- [x] 2 — 1.2 Implement locked apply-time re-evaluation, timestamped registry backup, checksum, receipt, and atomic removal of missing entries, and verify runtime data plus existing registrations are preserved. (COMPLETE)
- [x] 3 — 1.3 Add tests for dry-run byte immutability, apply output, path reappearance, repeat-run idempotence, shared-project replicas, and rollback evidence. (COMPLETE)
- [x] 4 — 2.1 Add `prometheus kbd projects --prune-missing [--apply]`, reject invalid flag combinations, and verify JSON and human output through focused CLI tests. (COMPLETE)
- [x] 5 — 2.2 Update operator documentation with dry-run, apply, backup, and rollback behavior, and verify command examples match clap flag placement. (COMPLETE)
- [x] 6 — 2.3 Run Rust formatting, clippy with warnings denied, focused/full affected crate tests, release builds, protected-test verification, and `git diff --check`, recording exact local results. (COMPLETE)

## reconcile-kbd-control-plane-projections — Preserve historical evidence while removing compatibility artifacts from live discovery and refreshing installed surfaces

- [x] 1 — 1.1 Confirm the canonical nested child and duplicate compatibility paths, preserve the compatibility directory under `.kbd-orchestrator/backups`, and write a receipt with source, destination, canonical target, and hashes. (COMPLETE)
- [x] 2 — 1.2 Verify live phase discovery no longer sees the compatibility path while the canonical nested record and backup evidence remain readable. (COMPLETE)
- [x] 3 — 2.1 Generate shared Codex/plugin distributions twice, prove identical tracked hashes, and run `npm run validate:codex` plus strict source validation locally. (COMPLETE)
- [x] 4 — 2.2 Refresh repository-managed user skill installations through the owned installer, verify source/install parity for touched skills, and prove user-owned collisions remain untouched. (COMPLETE)
- [x] 5 — 2.3 Build and install the tested release CLI binary with post-install version/hash verification. (COMPLETE)
- [x] 6 — 3.1 Run live KBD memory hook-write and recall probes, verify a retrievable lifecycle entity plus a non-unreachable digest, and record bounded cleanup/retention evidence. (COMPLETE)
- [x] 7 — 3.2 Run registry prune dry-run, apply it once, verify its backup receipt and idempotent second run, and confirm retained project runtimes still exist. (COMPLETE)
- [x] 8 — 3.3 Restart sovereign-sync twice under launchd and verify socket health, `prometheus doctor --check control.kbd-runtime`, `prometheus kbd status`, and zero unavailable-authority warnings in each new log window. (SKIPPED)
- [x] 8-local — 3.3 Keep sovereign-sync stopped and disabled, make its installation explicit to sharing workflows, then verify ordinary prometheus kbd status and typed mutations use the local signed runtime without unavailable-authority warnings. (COMPLETE)
- [x] 9 — 3.4 Run protected-test verification and `git diff --check`, then record all local commands/results in the phase evidence without using hosted CI. (COMPLETE)
