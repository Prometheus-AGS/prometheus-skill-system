# Goals

- Serve multiple KBD projects from one daemon
- Per-project bearer tokens instead of one global token
- Fence denials name the real cause and fix
- Narrow the Bash matcher so read-only commands are never fenced
