<!-- generated by kbd-runtime; source revision 36 -->
# sovereign-sync-domain-adapters tasks

## change-kbd-presence-peer-auth — Wire real peer authentication into kbd-control presence sync

- [x] 1 — Decide the authentication source (COMPLETE)
- [ ] 2 — Thread a real peer_authorized bool from handle_incoming_message into the kbd-control domain merge path (PENDING)
- [ ] 3 — Route kbd-control through KbdPresenceDocument::import_authenticated instead of the generic DomainAdapter::import_json path (PENDING)
- [ ] 4 — Add a test proving an unauthenticated peer's presence message is rejected (PENDING)
- [ ] 5 — cargo test -p sovereign-sync green (PENDING)

## change-learner-model-e2e-test — Add a learner-model end-to-end replication test

- [x] 1 — Add learner_model_replicates_end_to_end_between_two_nodes test (COMPLETE)
- [x] 2 — Seed a real LearnerModel on node A before pushing (COMPLETE)
- [x] 3 — Assert node B's LearnerModelStore::load reflects node A's content (COMPLETE)
- [x] 4 — cargo test -p sovereign-sync green (COMPLETE)

## change-verify-p2p-transport — Verify real P2P transport on real hardware

- [ ] No tasks registered

