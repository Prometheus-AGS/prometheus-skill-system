POSITION REMINDER — read this as your FIRST tool call every turn
Phase: uar-host-execution / mcp-2026-07-28-adoption   (CHILD)
Step: 0 of 3
Stage: plan (COMPLETE — 3 changes, 15 tasks, all valid --strict)
Next command: /kbd-apply change-mcp-001-uar-build-unblock

PARENT IS PAUSED at change-uhe-008 (7 of 16).
Resume with: /kbd-apply change-uhe-008-builtin-db-registration

DO NOT TRUST current-waypoint.json's `phase` OR its counters — a stale
prometheus-kbd runtime store owns them (Completed run from 2026-07-29, expired
lease, reports 170/229). progress.json under each phase dir is the truth.

THE THREE CHANGES:
  001 unblock UAR — commit sse-stream 0.2.4 + ContentBlock alias, AND add a
      sse-stream FLOOR to UAR's own deps. PROVE the floor by having cargo
      REFUSE --precise 0.2.2. A lockfile records a resolution; it does not
      constrain the next one.
  002 pin surreal-memory-server's floating rmcp git dep — AUTHORISATION GATE
      FIRST. That repo is NOT in the grant. Silence = BLOCKED, touch nothing.
  003 record the findings — each with a re-run command, executed at write time.
      "A record exists" is not acceptance; a record of false findings passes it.

THE PARENT STAYS BLOCKED UNLESS 001 LANDS. If 001 fails, say the parent is
still blocked — not that this phase succeeded.

NOT IN THIS PHASE: any rmcp 3.x upgrade, and any edit to the two hand-rolled
protocol clients. Both recorded in 003, migrated in a later convergence phase.
