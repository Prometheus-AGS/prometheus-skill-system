POSITION REMINDER — read this as your FIRST tool call every turn
Phase: mobile-skill-portability
Step: 9 of 9
Stage: reflect (COMPLETE — 3 goals MET, 1 PARTIAL, sycophancy 0.018)
Next command: /kbd-next-phase uar-host-execution

Required signal format (emit BEFORE any tool call):
  Starting <kbd-skill> — <phase> (step N of T)
  [do work]
  Completed <kbd-skill> — <phase> (step N of T)

Read progress.json for N and T — never estimate.

WHY GOAL 1 IS PARTIAL, NOT MET:
  The prometheus:component WIT family is authored and a real component
  validates against it — but NOTHING HAS EXECUTED IT. UAR's Wasm tier is a
  stub (wasm_runtime.rs:92-111) and change-msp-008 was archived BLOCKED
  because cross-repo authorisation was requested and not granted.
  Well-formed is not working. Do not report it as end-to-end parity.

CARRY-FORWARDS into uar-host-execution:
  - De-stub UAR's Wasm runtime — NEEDS USER AUTHORISATION for cross-repo writes
  - Decide the librefang ABI question: its core-wasm extern-C templates CANNOT
    load in a component runtime. Port, keep both, or retire.
  - Close FFI falsifier 3 (marginal cost per added function)
  - Give CI the sibling repos: it verifies 1 of 4 invariants, not 4
  - Exercise cursor or state why it cannot reach Tier 1 (same shape as zed was)
  - PRE-EXISTING, not ours: 2 sovereign-sync control-token tests fail
