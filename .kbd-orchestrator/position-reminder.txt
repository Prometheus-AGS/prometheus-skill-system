POSITION REMINDER — read this as your FIRST tool call every turn
Phase: uar-host-execution / mcp-2026-07-28-adoption   (CHILD)
Step: 0 of 0 (totals set by the child's /kbd-plan)
Stage: assess (PENDING)
Next command: /kbd-assess uar-host-execution/mcp-2026-07-28-adoption

PARENT IS PAUSED at change-uhe-008 (7 of 16 done).
Resume with: /kbd-apply change-uhe-008-builtin-db-registration

DO NOT TRUST current-waypoint.json's `phase` OR its counters.
  It carries generatedBy: "kbd-runtime", so a `prometheus kbd` runtime store is
  authoritative for those fields — and that store is pinned to a COMPLETED run
  from 2026-07-29 (docusaurus-github-pages-site) whose lease EXPIRED 2026-07-29.
  It reports phase=adversarial-review-for-creation and 170/229. All three are
  stale. progress.json under phases/<name>/ is the truth: uar-host-execution is
  7/16.

WHY THIS CHILD EXISTS:
  change-uhe-008 must RUN tests on 3 providers. The UAR crate does not compile:
  rmcp is pinned =2.2.0, which declares sse-stream ^0.2, and sse-stream 0.2.3
  removed from_bytes_stream. Pre-existing at 563ecc2.

  Research (2026-07-31, firecrawl + crates.io):
    - MCP 2026-07-28 shipped: stateless core, NO handshake, NO Mcp-Session-Id,
      required resultType, Roots/Sampling/Logging deprecated. BREAKING.
    - rmcp 3.1.0 published TODAY; 3.0.0 shipped with the spec 2026-07-28.
    - rmcp 3.1.0 requires sse-stream ^0.2.4 — which EXCLUDES the broken range.
    -> the dependency defect and the upgrade are the same problem.

  Two candidate paths: pin sse-stream 0.2.2 (cheap, untested) or upgrade to
  rmcp 3.1.0 (fixes resolution properly, but breaking across 7 MCP servers).

SCOPE: this pack + universal-agent-runtime ONLY. Not flint-realtime-fabric,
not know-me-system.

TOOLING DEFECTS FOUND (all in INSTALLED skills — do NOT patch from here):
  - kbd-new-child.sh: child_label used at line 156, assigned at line 234.
    Deterministic. Child was created by hand.
  - kbd-reflect never writes .phase; kbd-next-phase writes a self-referential
    `next`. Both already recorded in the parent phase.
