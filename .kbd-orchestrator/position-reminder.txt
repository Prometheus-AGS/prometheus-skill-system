POSITION REMINDER — read this as your FIRST tool call every turn
Phase: mobile-skill-portability
Step: 0 of 0 (totals set by /kbd-plan)
Stage: assess (PENDING — goals seeded, not yet assessed)
Next command: /kbd-assess mobile-skill-portability

Required signal format (emit BEFORE any tool call):
  Starting <kbd-skill> — mobile-skill-portability (step N of T)
  [do work]
  Completed <kbd-skill> — mobile-skill-portability (step N of T)

Read progress.json for N and T — never estimate.

SETTLED, DO NOT REOPEN:
  docs/decisions/wit-world-unification.md fixes the prometheus:component/* shape
  AND the ordering (unify BEFORE porting any skill). Implement it; don't re-decide.
  Divergence is 4 packages across 2 repos — knowme:plugin exists at 0.1.0 AND 1.0.0.

SCOPE GATE:
  The previous phase wrote ZERO files into flint-realtime-fabric,
  universal-agent-runtime, or know-me-system (verified by timestamp). Cross-repo
  code needs the user's explicit agreement first — it is not implied by the
  decision being recorded.

CARRY-FORWARDS:
  - 4 version invariants are DOCUMENTED, NOT ENFORCED (3 hold; nothing checks any)
  - Tier 1 verified on codex ONLY; opencode/kimi unrun; zed falls to Tier 0
  - PRE-EXISTING, not ours: 2 sovereign-sync control-token tests fail
  - "Demonstrated" is not "enforced" — criteria must make violation impossible
