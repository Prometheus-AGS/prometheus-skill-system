POSITION REMINDER — read this as your FIRST tool call every turn
Phase: uar-host-execution
Step: 0 of 16
Stage: plan (COMPLETE — 16 changes, 68 tasks, all valid --strict)
Next command: /kbd-apply change-uhe-001-cursor-tier1

Required signal format (emit BEFORE any tool call):
  Starting <kbd-skill> — uar-host-execution (step N of T)
  [do work]
  Completed <kbd-skill> — uar-host-execution (step N of T)

Read progress.json for N and T — never estimate.

SCOPE IS ELEVEN ITEMS: seeded S1-S6 + user-added R1-R5. Not five, not six.

ORDERING (in-repo cheap first, cross-repo after):
  W1 001 cursor · 002 CI siblings · 003 FFI cost · 004 librefang decision
  W2 005 pack provenance  <- blocks R5; nothing records the loaded version today
  W3 006 origin/enabled (PROBE first) -> 007 delete guard -> 008 all 3 providers
     009 SDK -> 010 REST per-verb -> 011 dynamic opt-in · 012 UI builtin
  W4 013 update check · 014 mobile transport · 015 WASM EXECUTION · 016 waypoint check

HARD RULES:
  - EVERY UAR claim is cross-repo. Each change RE-RUNS its own reproduction
    command before acting. Never inherit a claim from plan.md.
  - 015 is the PARITY GATE. Nothing is end-to-end parity until it passes.
  - 006 PROBES first (can a constraint target definition->>'origin'?). Either
    branch COMPLETES the change so downstream deps never dangle.
  - 008 needs ALL THREE providers. memory IS the embedded case — never skip it.
  - 016 delivers a report + in-repo CHECK, NOT a patch to installed skills.
  - 011 defaults OFF. "Optionally" must be encoded in the default.
  - Authorised: universal-agent-runtime ONLY. NOT flint-realtime-fabric,
    NOT know-me-system.

PRE-EXISTING, not ours: 2 sovereign-sync control-token tests fail.
