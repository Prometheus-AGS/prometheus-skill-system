{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "KBD Current Waypoint Compatibility Projection",
  "type": "object",
  "required": [
    "schemaVersion",
    "phase",
    "status",
    "implementationCompleted",
    "implementationTotal",
    "updatedAt"
  ],
  "properties": {
    "schemaVersion": { "const": "5" },
    "generatedBy": {
      "type": "string",
      "description": "kbd-runtime for authoritative replay projections; absent only during legacy shadow mode."
    },
    "sourceRevision": {
      "type": "integer",
      "minimum": 0,
      "description": "Canonical event revision used to render this projection."
    },
    "path": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "phase": { "type": "string", "minLength": 1 },
    "phaseIds": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "activePhaseId": { "type": ["string", "null"] },
    "activePhase": {
      "type": ["string", "null"],
      "description": "Slug of the final active phase in path. phase remains the root compatibility slug."
    },
    "previousPhase": { "type": ["string", "null"] },
    "change": { "type": ["string", "null"] },
    "status": {
      "description": "Legacy lifecycle alias in runtime projections; shadow inputs may contain stage-like values.",
      "enum": [
        "ready",
        "running",
        "pause_requested",
        "paused",
        "blocked",
        "completed",
        "cancelled",
        "failed",
        "assessment_ready",
        "assessed",
        "analyzed",
        "plan_complete",
        "execute_ready",
        "executing",
        "reflect_ready",
        "reflect_complete",
        "reflected"
      ]
    },
    "lifecycle": { "type": "string", "description": "Canonical run lifecycle, distinct from active stage." },
    "stageId": { "type": ["string", "null"], "description": "Active canonical stage ID; null when no stage is active." },
    "stageStatus": { "type": ["string", "null"], "description": "Active stage status, separate from run lifecycle." },
    "currentTask": { "type": ["string", "null"] },
    "nextPendingChange": { "type": ["string", "null"] },
    "sourceTool": { "type": ["string", "null"] },
    "nextChange": {
      "type": ["string", "null"],
      "description": "Next nonterminal change derived from canonical task state at sourceRevision."
    },
    "nextTask": {
      "type": ["string", "null"],
      "description": "Next nonterminal task derived from canonical task state at sourceRevision."
    },
    "exactNextCommand": {
      "type": ["string", "null"],
      "description": "Stored operator intent. It may be stale and must never select executable work."
    },
    "parentPhase": { "type": ["string", "null"], "description": "Immediate parent slug; null for top-level phases." },
    "childPhases": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "childPointer": { "type": ["string", "null"] },
    "backend": { "type": ["string", "null"] },
    "wave": { "type": ["integer", "null"], "minimum": 0 },
    "lastCompletedChange": { "type": ["string", "null"] },
    "completionMetric": { "const": "implementation" },
    "implementationCompleted": { "type": "integer", "minimum": 0 },
    "implementationTotal": { "type": "integer", "minimum": 0 },
    "certificationStatus": { "type": "string" },
    "publicationStatus": { "type": "string" },
    "planRevision": { "type": "integer", "minimum": 1 },
    "revision": { "type": "integer", "minimum": 0 },
    "updatedAt": { "type": "string", "format": "date-time" }
  },
  "allOf": [
    {
      "not": {
        "anyOf": [
          { "required": ["changes_completed"] },
          { "required": ["changes_total"] },
          { "required": ["exact_next_command"] },
          { "required": ["current_task"] },
          { "required": ["state_revision"] }
        ]
      }
    }
  ],
  "additionalProperties": true
}
