# Tasks

- [x] Add a coach agent under agents/ that advances the plan but never grades it
- [x] Reuse the existing reflector hook unmodified
- [x] Assert coach output is evaluated by the reflector, never by the coach
