# Tasks

- [x] PROBE FIRST: can a DB constraint target definition->>'origin'?
- [x] If YES: deliver that expression, add no columns — change still COMPLETES
- [x] ~~If NO: add origin + enabled columns~~ — N/A: the probe answered YES, so no columns were added
- [x] Provider round-trips whichever form was chosen; existing rows survive with correct values
