# Tasks

- [x] Add an explicit opt-in (flag or env) for registering generated skills
- [x] Default it OFF — the requirement says optionally, and the default must encode that
- [x] Test proves that WITHOUT the opt-in, no row is written
- [x] Test proves that WITH the opt-in, the skill is registered
