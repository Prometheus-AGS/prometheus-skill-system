# Tasks

- [x] Admin UI visually distinguishes builtin skills from user skills
- [x] Delete affordance is ABSENT or DISABLED for builtin skills — not a button that 409s
- [~] **WRITTEN, NOT EXECUTED** — 3 R3 tests added and parsing; Playwright `webServer` cannot start (unresolved `@prometheus-ags/prometheus-entity-management`, pre-existing — an *original* test from the same file fails identically). R3 = PARTIAL.
- [x] Confirm the toggle still works for builtin skills — disable is allowed, delete is not
