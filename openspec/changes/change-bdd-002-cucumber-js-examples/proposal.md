# Proposal — change-bdd-002-cucumber-js-examples

Add `references/examples/` to `skills/testing/bdd-cucumber-js/` with two
reference feature files illustrating the same business behavior tested
(a) via HTTP-only steps and (b) via Playwright-driven browser steps.
Reader learns when to pick which style.

## Goal
G-05 — Visual + non-visual scenario examples (TS side).
