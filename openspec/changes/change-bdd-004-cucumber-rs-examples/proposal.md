# Proposal — change-bdd-004-cucumber-rs-examples

Add `references/examples/` to `skills/testing/bdd-cucumber-rs/` with two
reference feature files: one HTTP-only (tokio + reqwest) and one browser
(thirtyfour). Same business behavior demonstrated both ways.

## Goal
G-05 — Visual + non-visual scenario examples (Rust side).
