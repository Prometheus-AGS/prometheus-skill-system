# Tasks — change-dsg-002-push-tag

- [ ] Write `.github/workflows/release.yml` in the dsg repo with cross-platform matrix
- [ ] Stage and commit the release workflow file
- [ ] Push all commits to `origin/main` (`git push origin main`)
- [ ] Create annotated tag `v0.1.0` and push it (`git push origin v0.1.0`)
- [ ] Verify remote has the tag: `git ls-remote --tags origin v0.1.0`
