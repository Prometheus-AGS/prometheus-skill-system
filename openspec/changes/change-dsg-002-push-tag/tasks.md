# Tasks — change-dsg-002-push-tag

- [x] Write `.github/workflows/release.yml` in the dsg repo with cross-platform matrix
- [x] Stage and commit the release workflow file
- [x] Push all commits to `origin/main` (`git push origin main`)
- [x] Create annotated tag `v0.1.0` and push it (`git push origin v0.1.0`)
- [x] Verify remote has the tag: `git ls-remote --tags origin v0.1.0`
