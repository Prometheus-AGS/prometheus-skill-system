# Tasks

- [ ] Add a coach agent under agents/ that advances the plan but never grades it
- [ ] Reuse the existing reflector hook unmodified
- [ ] Assert coach output is evaluated by the reflector, never by the coach
