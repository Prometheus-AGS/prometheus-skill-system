# Tasks

- [ ] Put iroh-docs behind a feature, default-on for native
- [ ] Raise the iroh floor to >=1.0.2 (1.0.2 fixed a relay DoS: one malformed datagram from any client crashed a whole relay)
- [ ] Native cargo build unchanged; wasm32 check progresses past the iroh-docs failure
