# Tasks — change-learn-004

- [ ] Implement `--kb` flag in `content-grounding-kb.sh` with adapter dispatch logic
- [ ] Add Dify KB adapter using `dify_search` and `dify_list_documents` MCP tools
- [ ] Add local file adapter that ingests via `palace_ingest` into surreal-memory
- [ ] Add surreal-memory palace adapter using `palace_search` and `palace_recall`
- [ ] Document privacy model: KB content stays local, never forwarded to external APIs or LLM grounding calls
