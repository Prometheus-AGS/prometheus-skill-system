# Tasks — change-learn-013

- [ ] Write `skills/learn/learn-practice/SKILL.md` with `--type derivation|implementation|transfer` flag, session protocol, and output contract
- [ ] Implement difficulty gating: mastery ≤ 0.6 serves foundational problems; mastery > 0.6 unlocks harder variants
- [ ] Implement interleaved problem type rotation: do not serve the same type twice in a row within a session
- [ ] Grade each response via `learn-grade` skill; surface feedback via ui-surface
- [ ] Produce `practice-result.json` (problems attempted, scores, types used) and update learner-model concept states
