# Tasks

- [ ] Set cursor TIER=tier1_structured in detect-surface-tier.sh (currently hardcoded tier0_text)
- [ ] Add cursor to BOTH Tier 1 dispatch lists in render.sh (direct + Tier2 fallback)
- [ ] RUN the file-pair round trip under cursor with an independent responder
- [ ] Confirm under bash -x that dispatch reaches _render_tier1_file_pair
- [ ] OR: record the diagnostic and keep cursor documented as Tier 0, making no Tier 1 claim
