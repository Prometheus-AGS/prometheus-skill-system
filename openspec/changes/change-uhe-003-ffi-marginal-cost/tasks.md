# Tasks

- [ ] Add a second function to substrate/skill-ffi
- [ ] Count the hand-written Dart + Rust annotation + build-config lines it required
- [ ] Record the count against the decision's ~20-line threshold
- [ ] If over threshold: reverse the FFI pattern decision and record the reversal via decision-log.sh outcome
