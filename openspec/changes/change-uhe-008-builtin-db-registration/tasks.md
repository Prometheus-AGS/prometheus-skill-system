# Tasks

- [ ] After startup, builtin row count == loader discovered count on postgres
- [ ] Same equality on surreal
- [ ] Same equality on memory — this IS the embedded case, do not skip it
- [ ] Any provider that cannot be exercised: archive BLOCKED naming the prerequisite, report R1 PARTIAL
