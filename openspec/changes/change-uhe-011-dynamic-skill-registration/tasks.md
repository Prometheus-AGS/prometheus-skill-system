# Tasks

- [ ] Add an explicit opt-in (flag or env) for registering generated skills
- [ ] Default it OFF — the requirement says optionally, and the default must encode that
- [ ] Test proves that WITHOUT the opt-in, no row is written
- [ ] Test proves that WITH the opt-in, the skill is registered
