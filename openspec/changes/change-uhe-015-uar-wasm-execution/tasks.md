# Tasks

- [ ] De-stub wasm_runtime.rs:92-111 — instantiate and invoke the component
- [ ] The reference component returns ITS OWN output, not the placeholder string
- [ ] Test asserts on the returned value, not merely that a call completed
- [ ] Only now may goal S1 move from PARTIAL to MET
