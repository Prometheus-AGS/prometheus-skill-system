# Tasks

## 1. Recover and integrate

- [x] 1.1 Inventory both repositories and preserve main-checkout baselines; record dirty worktree paths.
- [ ] 1.2 Preserve the original patch in a named branch and port double-colon support without removing current separators; inspect the resulting diff.

## 2. Final integration boundary

- [ ] 2.1 Exercise all qualified forms through the real CLI integration target; record the exact local command and result.
- [ ] 2.2 Confirm secondary worktrees are clean and baseline main changes remain untouched; commit only the recovered work and its record.
