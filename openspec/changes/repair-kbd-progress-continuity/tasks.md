## 1. Canonical Position Continuity

- [x] 1.1 Derive the active change/task cursor during phase activation and change/task folding, then render the same next work in JSON, Markdown, reminders, and human status.
- [x] 1.2 Remove exact-next operator prose from selection paths and pass qualified change/task subjects through completion and bottleneck evaluation.
- [x] 1.3 Pass compiled CLI integration coverage for completed-task advancement, repeated IDs, stale intent, nested phase exit, restart, and resume.

## 2. Durable Progress Memory

- [x] 2.1 Add the standalone `karpathy-progress-memory` skill, versioned event schema, canonical identity validation, idempotent session log, project-scoped `pk` ingestion, and durable outbox fallback.
- [x] 2.2 Wire task, change, and phase boundary drivers so only successful terminal transitions can invoke completion recording.
- [x] 2.3 Pass temporary-project integration coverage for successor-run identity, replay after exact-next/HEAD/worktree changes, crash recovery before and after memory submission, outage, restart, identity disagreement, and projection non-mutation.

## 3. Local Acceptance

- [x] 3.1 Pass strict OpenSpec and skill validation.
- [x] 3.2 Pass affected KBD runtime, CLI, hooks, position, and distribution integration targets locally.
- [x] 3.3 Record commands, exit codes, observed counts, critic findings, and commit hash in the recovery evidence.
