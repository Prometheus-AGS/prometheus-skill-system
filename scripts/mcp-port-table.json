{
  "comment": "Source of truth for Prometheus MCP service connectivity. HTTP services have port+path. stdio services have command+args.",
  "services": {
    "surreal-memory": {
      "type": "sse",
      "url": "http://localhost:23001/mcp/sse",
      "port": 23001,
      "path": "/mcp/sse",
      "transport": "http",
      "description": "Surreal-memory distributed knowledge graph, scoped memory, TaskStreams, Graph-RAG"
    },
    "prometheus-knowledge": {
      "type": "sse",
      "url": "http://localhost:8942/mcp",
      "port": 8942,
      "path": "/mcp",
      "transport": "http",
      "description": "pk-cherry HTTP MCP — Karpathy LLM Knowledge Base (knowledge_ingest, knowledge_focus, knowledge_search)"
    },
    "forge-rs": {
      "type": "sse",
      "url": "http://localhost:8943/mcp",
      "port": 8943,
      "path": "/mcp",
      "transport": "http",
      "description": "Forge code-enrichment engine (forge_enrich, forge_reflect, forge_drift, forge_validate)"
    },
    "sycophancy-correction": {
      "type": "stdio",
      "command": "/usr/local/bin/sycophancy-correction",
      "args": [],
      "transport": "stdio",
      "description": "Sycophancy detection and correction (detect_sycophancy, correct_sycophancy, analyze_reflect_phase)"
    },
    "liter-llm": {
      "type": "stdio",
      "command": "/Users/gqadonis/.local/bin/liter-llm",
      "args": ["mcp", "--transport", "stdio"],
      "transport": "stdio",
      "description": "Multi-model routing proxy — 22 MCP tools across 140+ providers"
    },
    "sequential-thinking": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "transport": "stdio",
      "description": "Sequential thinking MCP for structured step-by-step reasoning"
    },
    "tavily": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/tavily-mcp@latest"],
      "env": {"TAVILY_API_KEY": "${TAVILY_API_KEY}"},
      "transport": "stdio",
      "description": "Tavily web search MCP (tavily_search, tavily_extract, tavily_research) — search-first, ranked result aggregation"
    },
    "firecrawl": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_URL": "${FIRECRAWL_API_URL:-http://localhost:3002}",
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY:-}"
      },
      "transport": "stdio",
      "description": "Firecrawl web data pipeline MCP — 13 tools: scrape, crawl, map, extract, search, interact. Self-hosted at Prometheus AGS (AGPL-3.0). Extraction-first; use for full page content, PDFs, dynamic UIs. Use tavily for search-first discovery."
    }
  }
}
