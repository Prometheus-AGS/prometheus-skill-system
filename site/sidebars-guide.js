// Sidebar for the canonical guide instance (../docs/guide).
// Numbered NN- filename prefixes drive the order; Docusaurus strips them
// from ids/URLs automatically.
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  guideSidebar: [{ type: 'autogenerated', dirName: '.' }],
};

module.exports = sidebars;
