[package]
name = "prometheus-skill-index"
version = "1.7.0"
edition = "2021"
publish = false
description = "Deterministic shared skill index and selection contract"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
