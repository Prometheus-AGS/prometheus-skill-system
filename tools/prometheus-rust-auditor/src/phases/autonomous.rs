use crate::AppContext;
use anyhow::Result;

pub fn run(_ctx: &AppContext) -> Result<i32> {
    // TODO: Phases 6–9 — AI audit loop via `claude --headless`
    eprintln!("autonomous mode: not yet implemented");
    Ok(0)
}
